# Basic Authentication Configuration
# Copy this file to .env and update with your values for local testing
# These variables are used by server.cjs for basic authentication

# Username for basic auth (leave empty to disable auth)
BASIC_AUTH_USER=admin

# Password for basic auth (leave empty to disable auth)
BASIC_AUTH_PASSWORD=changeme

# Server port (defaults to 4173 if not set)
PORT=4173